define host {
    use                     ${use}
    host_name               ${name}
    alias                   ${alias}
    address                 ${ip}
    hostgroups              ${groups}
    }
