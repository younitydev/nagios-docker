define service {
    use                             ${use}
    host_name                       ${names}
    service_description             PING
    check_command                   check_ping!100.0,10%!300.0,20%
    }
